# Project Alpha - AWS S3 Terraform Static Site

This project uses **Terraform** to create an AWS S3 bucket that hosts a static website.

## How to Use

1. Go to the `infra/` folder
2. Run:
terraform init
terraform plan
terraform apply
3. When finished, to delete everything:
terraform destroy